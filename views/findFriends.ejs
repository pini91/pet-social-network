<%- include('partials/header') -%>   <!--we have a header and footerpartial in the view/partials so we dont have to repeat our selfs. Where we put our bootstrap -->
  <div class="container">
    <a class="btn btn-primary col-2 mt-5" href="/feed">Go to Feeds</a>
       <div class="d-flex justify-content-end">
      <a href="/logout" class="btn btn-gradient-danger">
        <i class="fas fa-sign-out-alt me-2"></i>Logout
      </a>
    </div>
    <div>
      <form class="" action="/search" method="POST">
        <input type="text" placeholder="Search.." name="search">
        <button type="submit"><i class="fa fa-search"></i></button>
        <!-- <i style="font-size:24px" class="fa"><a class="refresh" href=""></a></i> -->
      </form>
      <h3 class="mt-4">Friends</h3>
    </div>
    <div>
    <div class="d-flex justify-content-center mt-3">
      <ul class="row list-unstyled">
        <% for(var i=0; i<friends.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">

            <% const profilePic = photos.find(photo => photo.user.toString() === friends[i]._id.toString()); %>
             <img
                class="profile-picture-large"
                src="<%= profilePic ? profilePic.image : '' %>"
                alt="User Image"
              />


              <a href="/friends/<%= friends[i]._id %>" class="friend-name"> 
                <h3 class="text-capitalize"><%= friends[i].userName %></h3>
              </a>
          </li>
        <% } %>
        </ul>
    </div>
  </div>  
<%- include('partials/footer') -%>